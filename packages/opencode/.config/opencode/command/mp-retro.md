---
description: "Conduct a retrospective on a coding session."
---

Use the mp-retro skill (~/.claude/skills/mp-retro/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
