---
description: "Turn a decision you can't fully answer into a questionnaire for someone else to fill in."
---

Use the mp-to-questionnaire skill (~/.claude/skills/mp-to-questionnaire/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
