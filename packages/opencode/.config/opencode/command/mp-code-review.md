---
description: "Review the changes since a fixed point along two axes: Standards (repo coding standards) and Spec (originating issue/spec), run as parallel sub-agents and reported side by side."
---

Use the mp-code-review skill (~/.claude/skills/mp-code-review/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
