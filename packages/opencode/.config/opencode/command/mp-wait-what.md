---
description: "Stop. That last message did not land: re-pitch it."
---

Use the mp-wait-what skill (~/.claude/skills/mp-wait-what/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
