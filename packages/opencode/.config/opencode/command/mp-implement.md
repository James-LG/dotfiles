---
description: "Implement a piece of work based on a spec or set of tickets."
---

Use the mp-implement skill (~/.claude/skills/mp-implement/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
