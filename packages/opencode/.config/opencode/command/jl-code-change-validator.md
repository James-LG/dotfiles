---
description: Validate recent code changes and write regression tests for what breaks
---

Use the jl-code-change-validator skill (~/.claude/skills/jl-code-change-validator/SKILL.md): read it plus any files it references, then follow it exactly.

Changes to validate: $ARGUMENTS (if empty, validate the current uncommitted changes).
