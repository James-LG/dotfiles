---
description: "Implement a specification in code."
---

Use the mp-implement-spec skill (~/.claude/skills/mp-implement-spec/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
