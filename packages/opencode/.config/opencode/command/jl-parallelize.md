---
description: Fan the independent parts of the current work out to concurrent subagents
---

Use the jl-parallelize skill (~/.claude/skills/jl-parallelize/SKILL.md): read it plus any files it references, then follow it exactly.

Work to parallelize: $ARGUMENTS (if empty, use the current task list or plan).
