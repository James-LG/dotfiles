---
description: "Writing, exploit; assemble raw material into a journey of beats, grounding each term before a beat leans on it."
---

Use the mp-writing-beats skill (~/.claude/skills/mp-writing-beats/SKILL.md): read it plus any files it references, then follow it exactly.

Arguments: $ARGUMENTS (if empty, follow the skill's own default.)
